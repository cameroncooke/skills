Shortlist step (agent-only, before user choice):

1) Classify every candidate as KEEP or REJECT using SKILL.md rubric.
2) Present only KEEP candidates to the user.
3) Include a brief filtered summary (count + reasons) for REJECT items.
4) Explicitly reject business-logic-specific one-offs that do not generalize beyond a single feature/path.

User choice:

- `all`
- `none`
- comma-separated IDs (example: `C001,C004,C009`)

Always show the exact proposed bullet text for each shortlisted candidate.
Optional: offer wording edits before write.
